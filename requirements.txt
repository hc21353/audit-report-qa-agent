sentence-transformers>=2.7.0
chromadb>=0.5.0
faiss-cpu>=1.8.0
torch>=2.0.0
transformers>=4.40.0

# DB / Data
pandas>=2.0.0
numpy>=1.26.0

# Parsing (Step 1 - already done, but keep for reproducibility)
beautifulsoup4>=4.12.0
lxml>=5.0.0
markdownify>=0.12.0

# CI/CD & Dev
pytest>=8.0.0
python-dotenv>=1.0.0

# App (Step 4)
streamlit>=1.35.0
openai>=1.30.0       # or anthropic>=0.25.0
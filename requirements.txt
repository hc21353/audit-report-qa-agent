# ============================================================
# Samsung Audit Report QA Agent - 의존성 패키지
# ============================================================
#
# ⚠️ 필수 요구사항: Ollama 설치
# 이 프로젝트는 Ollama LLM 서버가 필요합니다.
# 설치: https://ollama.ai
# 모델: ollama pull qwen2.5:14b (또는 fredrezones55/qwen3.5-opus:9b-tooling)
# 실행: ollama serve (포트: 11434)
#
# ============================================================

# 1. 핵심 설정 및 유틸리티
pyyaml>=6.0                          # YAML 설정 파일 파싱
python-dotenv>=1.0.0                 # .env 환경 변수 로드

# 2. LLM & 멀티에이전트 프레임워크
langgraph>=0.6.0                     # LangGraph 멀티에이전트 워크플로우
langchain>=0.3.0                     # LangChain 기본 프레임워크
langchain-core>=0.3.0                # LangChain 코어
langchain-community>=0.3.0           # LangChain 커뮤니티 통합 (Ollama 등)
langchain-ollama>=0.1.0              # LangChain Ollama 통합 (ChatOllama)

# 3. 임베딩 & 벡터 DB
sentence-transformers>=2.2.0         # KoE5 한국어 임베딩 모델
torch>=2.0.0                         # PyTorch (sentence-transformers 의존)
faiss-cpu>=1.7.4                     # 백터 유사도 검색
chromadb>=1.5.0                      # ChromaDB 벡터스토어

# 4. UI & 시각화
streamlit>=1.30.0                    # Streamlit 채팅 인터페이스
plotly>=5.18.0                       # Plotly 차트 생성

# 5. 웹 & 검색
ddgs>=1.0.0                          # DuckDuckGo 웹 검색 API
requests>=2.31.0                     # HTTP 요청

# 6. 데이터 처리
pandas>=2.0.0                        # 데이터 테이블 처리
numpy>=1.24.0                        # 수치 계산

# 7. 테스트 & 개발 도구
pytest>=7.0.0                        # 테스트 프레임워크
pytest-cov>=4.0.0                    # 코드 커버리지